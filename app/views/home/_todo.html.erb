<ul class="todo-list">
  <h2 class="todo-title">
    <%= t("todo") %>
    <span class="time-now">
      <%= t("datetime.today") %>
      <span class="clock"></span>
    </span>
  </h2>
  <% attended_events.each do |e| %>
    <li class="todo-item clearfix">
      <div class="e-title">
        <%= link_to e.title, event_path(uid: e.uid) %>
      </div>
      <div class="e-time">
        <%= render partial: "events/event_start", locals: { event: e } %>
      </div>
    </li>
  <% end %>
</ul>

<% content_for :template_js do %>
  <script>
    var updateClock = function() {
      function locale(){
        var c = document.cookie.split('; ');
        var tmp;
        for(var i = 0; i < c.length; i++) {
          tmp = c[i].split('=');
          if(tmp[0] == "locale") {
            break;
          }
        }
        return tmp[1];
      }
      var time;
      if(locale() == 'en') {
        moment.lang("en-ca")
        time = moment().tz("Asia/Chongqing").format("h:mm a, MMM d");
      } else {
        moment.lang("zh-cn")
        time = moment().tz("Asia/Chongqing").format("MMMD日 LT");
      }

      $('.clock').html(time);

      var now = new Date();
      var delay = 1000 - (now % 1000);
      setTimeout(updateClock, delay);
    };

    updateClock();
  </script>
<% end %>
