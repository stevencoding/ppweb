<div class="footer clearfix">
  <div class="container">
  <% if cookies[:locale] == "en" %>
    <%= link_to "简体中文", set_locales_url(:locale => "zh-CN"), class: "locale-btn" %>
  <% else %>
    <%= link_to "English", set_locales_url(:locale => "en"), class: "locale-btn" %>
  <% end %>
  </div>
</div>