<div class="footer clearfix">
  <div class="container">
    <div class="left-footer">
      <%= link_to t("contact"), "/contact" %>
    </div>
    <div class="right-footer">
      <% if cookies[:locale] == "en" %>
        <%= link_to "简体中文", "?locale=zh-CN", class: "locale-btn" %>
      <% else %>
        <%= link_to "English", "?locale=en", class: "locale-btn" %>
      <% end %>
    </div>
  </div>
</div>